# How to run

Command to build:
wasm-pack build --target web

Command to run locally (Assuming you have Node.js installed):
npx serve .

# Features to consider
Edit several images at the same time

Convert file type of images